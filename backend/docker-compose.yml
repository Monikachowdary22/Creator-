version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: creatoriq-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: creatoriq
    ports:
      - "5432:5432"